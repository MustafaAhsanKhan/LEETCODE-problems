#include <iostream>
using namespace std;
#include <stack>

class Solution {
public:
    string decodeString(string s)
    {
        int repeat = 0;
        for(int i = 0; i < s.length(); ++i)
        {
            if(s[i] == )
        }
    }
};